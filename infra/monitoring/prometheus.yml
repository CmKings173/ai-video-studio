global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: ai-video-studio-api
    metrics_path: /api/v1/metrics
    bearer_token_file: /etc/prometheus/studio-metrics.token
    static_configs:
      - targets: ["api:8000"]
